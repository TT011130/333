const setCounter = (el) => {
    const elements = document.querySelectorAll(el);

    //elements.forEach(el) => {
    //    const target = el.dataset.to;
    //    const suffix = el.dataset.suffix || "+";
   // }
}